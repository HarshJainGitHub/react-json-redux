
const initalState = {
    users:[],
    user:{},
    loading:false
}


const userReducer = (state =initalState , action) => {
    switch(action.type){
        default: return state
    }
}

export default userReducer;